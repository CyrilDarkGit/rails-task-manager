<h1>Tasks view</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> | <%= task.completed %> </li>
  <% end %>
</ul>
<br>
<%= link_to "New task", new_task_path %>
